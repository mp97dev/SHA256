name: SHA256 Service Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v4
      - name: ⚙️ Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake
      - name: 🧪 Run test script
        run: |
          bash ./run_test.sh
